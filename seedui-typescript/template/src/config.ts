export const authConfig = {
	adopterId: "${ADOPTER_ID}",   
	adopterApplicationName: "${ADOPTER_APPLICATION_NAME}",             
	applicationId: "${APPLICATION_ID}",
    inviteCount: "${INVITE_COUNT}"  // numbers
}
